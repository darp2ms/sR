name: worker

on:
  push:

jobs:
  tour:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        immeasurable: ["European", "essential", "outstanding", "requirement", "dictate"]
        yield: ["afford", "supreme", "selfish", "revenue"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Cr
